# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
spa-dist
# dist — مُتاح لـ Capacitor (capacitor.config.ts → webDir: dist)
!dist/
!dist/**
_vercel_site
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
.env.*
backups-local/
!.env.example
mobile/.env

# Vercel static bundle (generated on deploy)
_vercel_site

# Capacitor iOS — المشروع الأصلي مُرفوع (ios/.gitignore يستثني public و Pods)
# spa-dist يُبنى محلياً/على CI؛ لا تُرفع

# App binaries for landing (place files locally, do not commit large builds)
landing/public/downloads/*.ipa
!landing/public/downloads/retweet.ipa
landing/public/downloads/*.apk
build/ios/

# Wrangler / Cloudflare
.wrangler/
.dev.vars
cloudflare/tunnel.yml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Local dev / tunnel (do not publish)
LOCAL_URLS.txt
PUBLIC_API_URL.txt
PUBLIC_LINKS.txt
PUBLIC_PWA_URL.txt
PUBLIC_TUNNEL_URL.txt
.tunnel-log.txt
tunnel-err.txt
mobile/dist-ipa/*.ipa

# Local DB snapshots / IPA inspect temp (do not publish)
backend/data/
.ipa-inspect/
.ipa-inspect.zip
.ipa-package-work/
.user-ipa-inspect/
.ipa-verify/
.ipa-verify.zip
.ipa-fixed-check/
.ipa-fixed-check2/
.ipa-user-fix/
ios/build/
.vercel-deploy-dir.txt
dist/*.ipa
